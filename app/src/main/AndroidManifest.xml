<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET is required, and only for map tiles (DESIGN.md 9, D-037).
      Note this is a real change from the M0 spike, which had no network access at
      all. Photo data still never leaves the device; the tile host sees which part
      of the map is being viewed, and nothing else. Do not let this grow.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Broad library access. Requires a Play declaration; see DESIGN.md 10. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android 14+ Curated tier. Verified to return unredacted GPS (D-024). -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!--
      Without this, MediaStore strips GPS silently and every photo looks
      un-geotagged, with no error raised. See D-023.
    -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhotoGlobe">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.PhotoGlobe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

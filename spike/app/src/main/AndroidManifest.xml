<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      There is deliberately NO android.permission.INTERNET here.
      Without it, Android blocks all network access at the OS level - this app
      physically cannot send anything anywhere, regardless of what the code says.
      That is enforced by the operating system, not by the author.
    -->

    <!-- Android 13+ : broad read access to the image library. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!--
      Android 14+ : the partial / "Curated" grant. Declaring it alongside
      READ_MEDIA_IMAGES is what makes the system offer the three-option dialog
      (Allow all / Select photos / Don't allow).
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!--
      THE ONE THAT MATTERS. Without this, MediaStore returns EXIF with the GPS
      tags stripped and raises no error at all - every photo simply looks
      un-geotagged. That silent failure is what this spike exists to rule out.
    -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Android 12L and below. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="false"
        android:label="PhotoGlobe Spike"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
